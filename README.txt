To Compile:

1. navigate to the directory .../Backend_Assignment output
  in the command line
2. type "make" into the prompt

To Run:

1. Ensure that all appropriate .csv files are in the same
  directory as the executable file (Backend)
2. type ./Backend
3. the resulting output will be stored in the file report_card.txt
